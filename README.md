# Practice PRF v20

This is a clean version using uniquely named `app-v20.js` and `styles-v20.css` files with version query strings, so GitHub Pages and browsers cannot confuse them with previous `app.js` and `styles.css` files. A visible v20 badge appears beside the page title.

NEWS2 is implemented directly in the main observation builder using separate constants for AVPU, pupils, GCS and NEWS2. Pupils are Left/Right 0–10 mm. The sepsis image displays whenever any NEWS2 score is 5 or above.

Replace the empty `assets/New Incident.mp3` with the real MP3. Keep any existing `assets/als-guidelines-checklist.pdf` in the repository because it was not included in the uploaded source set.
