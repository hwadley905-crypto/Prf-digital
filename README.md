# Practice PRF v16 — NEWS2 fixed

This package fixes the observation-row closure fault that caused AVPU, pupils, GCS and NEWS2 controls to share the final button reference. NEWS2 now has its own stable button and opens the NEWS2 modal. Selecting cells updates the total; Done writes the score back to the same NEWS2 cell and applies the severity colour.

Replace the empty `assets/New Incident.mp3` placeholder with your real file. Upload all files and the complete `assets` folder to the GitHub repository root.
