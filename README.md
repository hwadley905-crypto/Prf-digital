# Practice PRF v22

Adds an **Exit case** button on the Sign Off page. It offers **Save and exit**, **Exit without saving**, or **Continue editing**, then returns to the Ready to begin screen.

Versioned filenames prevent older JavaScript/CSS from being reused. Replace the empty `assets/New Incident.mp3` placeholder with the real audio. Preserve or re-upload `assets/als-guidelines-checklist.pdf` if required.
